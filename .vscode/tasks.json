{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Verify vibe coding environment",
      "type": "shell",
      "command": "dotnet --version; az version; gh --version; if (Get-Command copilot -ErrorAction SilentlyContinue) { copilot --version } else { Write-Host 'copilot CLI not found. Install/configure copilot CLI.' }"
    },
    {
      "label": "Copilot terminal quick test",
      "type": "shell",
      "command": "copilot -p 'explain dotnet build'"
    },
    {
      "label": "GitHub CLI auth status",
      "type": "shell",
      "command": "gh auth status"
    }
  ]
}
